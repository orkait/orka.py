"""argparse-based CLI: build_parser + cmd_* dispatchers + main entry."""

from orka._impl import (
    build_parser,
    cmd_calc,
    cmd_eval,
    cmd_eval_sweep,
    cmd_inspect,
    cmd_kaggle_pack,
    cmd_pack,
    cmd_reconstruct,
    cmd_report,
    cmd_slrq_eval,
    cmd_sweep,
    cmd_verify,
    main,
)
